<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>毛珑珑的博客</title><link>https://chensl.me/blog/coreutils/</link><description>Recent content in coreutils on 毛珑珑的博客</description><generator>Hugo -- gohugo.io</generator><language>zh-Hans</language><managingEditor>shaolong.chen@outlook.it (Shaolong Chen)</managingEditor><webMaster>shaolong.chen@outlook.it (Shaolong Chen)</webMaster><lastBuildDate>Mon, 15 Jan 2024 21:12:35 +0800</lastBuildDate><atom:link href="https://chensl.me/blog/coreutils/index.xml" rel="self" type="application/rss+xml"/><item><title>MacOS 上更安全的 rm</title><link>https://chensl.me/trash-cli/</link><pubDate>Mon, 15 Jan 2024 21:12:35 +0800</pubDate><author>shaolong.chen@outlook.it (Shaolong Chen)</author><guid>https://chensl.me/trash-cli/</guid><description>rm 命令应该是 Unix 系统最危险的命令，没有之一。用 rm 删除文件后很难恢复，尤其是大家经常拿来开玩笑骗小白的 rm -rf /*。
真那么危险吗？ (这部分的命令千万别复制执行！！！)
真的！就身边朋友遇到的，或者听说到的，主要有两类：
错误的环境变量 # 定义了某个环境变量 export foo=xxx # 然后由于 typo，或者变量被 unset，最终引用了一个空的值 # 这条命令完全等价于 rm -rf /* rm -rf ${bar}/* 空格 乱在网上复制代码直接执行特别容易踩这个坑，浏览器上不注意看，以为是 /usr/local/bin/xxx，复制一执行才发现是 /usr 和 /local/bin/xxx。
rm -rf /usr /local/bin/xxx 其他手滑操作 终端自动补全，还没看清补全了什么，就直接回车（我就干过，还是一个没用 git 管理的目录 😇）：
rm -rf &amp;lt;Tab&amp;gt;&amp;lt;Tab&amp;gt;&amp;lt;Enter&amp;gt; MacOS 的回收站 因为平常基本用的 MacOS，自然很容易想到，不使用 rm 真正删除文件，而是把文件往回收站丢不就好了？
思路很简单，但是 MacOS 的回收站目录 (~/.Trash) 有很奇怪的权限（没仔细研究过），似乎没办法直接用 ls, cp, mv 之类的操作：
$ ls &amp;#34;.&amp;#34;: Operation not permitted (os error 1) $ sudo ls ls: cannot open directory &amp;#39;.</description><content:encoded><![CDATA[<p><code>rm</code> 命令应该是 Unix 系统最危险的命令，没有之一。用 <code>rm</code> 删除文件后很难恢复，尤其是大家经常拿来开玩笑骗小白的 <code>rm -rf /*</code>。</p>
<h2 id="真那么危险吗">真那么危险吗？</h2>
<p>(这部分的命令千万别复制执行！！！)</p>
<p>真的！就身边朋友遇到的，或者听说到的，主要有两类：</p>
<h3 id="错误的环境变量">错误的环境变量</h3>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># 定义了某个环境变量</span>
</span></span><span class="line"><span class="cl"><span class="nb">export</span> <span class="nv">foo</span><span class="o">=</span>xxx
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># 然后由于 typo，或者变量被 unset，最终引用了一个空的值</span>
</span></span><span class="line"><span class="cl"><span class="c1"># 这条命令完全等价于 rm -rf /*</span>
</span></span><span class="line"><span class="cl">rm -rf <span class="si">${</span><span class="nv">bar</span><span class="si">}</span>/*</span></span></code></pre></div>
<h3 id="空格">空格</h3>
<p>乱在网上复制代码直接执行特别容易踩这个坑，浏览器上不注意看，以为是 <code>/usr/local/bin/xxx</code>，复制一执行才发现是 <code>/usr</code> <strong>和</strong> <code>/local/bin/xxx</code>。</p>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">rm -rf /usr /local/bin/xxx</span></span></code></pre></div>
<h3 id="其他手滑操作">其他手滑操作</h3>
<p>终端自动补全，还没看清补全了什么，就直接回车（我就干过，还是一个没用 git 管理的目录 😇）：</p>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">rm -rf &lt;Tab&gt;&lt;Tab&gt;&lt;Enter&gt;</span></span></code></pre></div>
<h2 id="macos-的回收站">MacOS 的回收站</h2>
<p>因为平常基本用的 MacOS，自然很容易想到，不使用 <code>rm</code> 真正删除文件，而是把文件往回收站丢不就好了？</p>
<p>思路很简单，但是 MacOS 的回收站目录 (<code>~/.Trash</code>) 有很奇怪的权限（没仔细研究过），似乎没办法直接用 <code>ls</code>, <code>cp</code>, <code>mv</code> 之类的操作：</p>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ ls
</span></span><span class="line"><span class="cl"><span class="s2">&#34;.&#34;</span>: Operation not permitted <span class="o">(</span>os error 1<span class="o">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">$ sudo ls
</span></span><span class="line"><span class="cl">ls: cannot open directory <span class="s1">&#39;.&#39;</span>: Operation not permitted</span></span></code></pre></div>
<p>Google 了一波，发现可以用 AppleScript 实现类似 <code>mv file ~/.Trash/</code> 的操作:</p>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># https://apple.stackexchange.com/a/310084</span>
</span></span><span class="line"><span class="cl">osascript -e <span class="s2">&#34;tell application \&#34;Finder\&#34; to delete POSIX file \&#34;</span><span class="si">${</span><span class="nv">PWD</span><span class="si">}</span><span class="s2">/</span><span class="si">${</span><span class="nv">InputFile</span><span class="si">}</span><span class="s2">\&#34;&#34;</span></span></span></code></pre></div>
<h2 id="trash-cli">trash-cli</h2>
<p><a href="https://github.com/maolonglong/trash">完整代码</a></p>
<p>最后就是把它包装成一个好用的 CLI，保持了和 <code>rm</code> 差不多的选项，所以简单起见，可以直接 <code>alias rm=trash</code>。</p>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ go install github.com/maolonglong/trash@latest
</span></span><span class="line"><span class="cl">...
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">$ trash --help
</span></span><span class="line"><span class="cl">Usage: trash <span class="o">[</span>OPTION<span class="o">]</span>... <span class="o">[</span>FILE<span class="o">]</span>...
</span></span><span class="line"><span class="cl">Move FILE<span class="o">(</span>s<span class="o">)</span> to the trash.
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  -f, --force           ignore nonexistent files and arguments, never prompt
</span></span><span class="line"><span class="cl">  -h, --help            display this <span class="nb">help</span> and <span class="nb">exit</span>
</span></span><span class="line"><span class="cl">  -p, --protect paths   protected paths, separated by comma
</span></span><span class="line"><span class="cl">  -r, --recursive       remove directories and their contents recursively
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">By default, trash does not remove directories.  Use the --recursive <span class="o">(</span>-r<span class="o">)</span>
</span></span><span class="line"><span class="cl">option to remove each listed directory, too, along with all of its contents.
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">To remove a file whose name starts with a <span class="s1">&#39;-&#39;</span>, <span class="k">for</span> example <span class="s1">&#39;-foo&#39;</span>,
</span></span><span class="line"><span class="cl">use one of these commands:
</span></span><span class="line"><span class="cl">  trash -- -foo
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  trash ./-foo</span></span></code></pre></div>
<p>注意有个 <code>protect</code> 参数（可以理解成是 deny list），分享下我自己的用法：</p>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># 配置文件是可选的，位置也随意，甚至可以不需要配置文件，</span>
</span></span><span class="line"><span class="cl"><span class="c1"># 不管什么方式，只要传了 protect 参数就行。</span>
</span></span><span class="line"><span class="cl">$ cat ~/.protected_paths
</span></span><span class="line"><span class="cl">/
</span></span><span class="line"><span class="cl">/bin
</span></span><span class="line"><span class="cl">/boot
</span></span><span class="line"><span class="cl">/dev
</span></span><span class="line"><span class="cl">/etc
</span></span><span class="line"><span class="cl">/home
</span></span><span class="line"><span class="cl">/initrd
</span></span><span class="line"><span class="cl">/lib
</span></span><span class="line"><span class="cl">/lib32
</span></span><span class="line"><span class="cl">/lib64
</span></span><span class="line"><span class="cl">/proc
</span></span><span class="line"><span class="cl">/root
</span></span><span class="line"><span class="cl">/sbin
</span></span><span class="line"><span class="cl">/sys
</span></span><span class="line"><span class="cl">/usr
</span></span><span class="line"><span class="cl">/usr/bin
</span></span><span class="line"><span class="cl">/usr/include
</span></span><span class="line"><span class="cl">/usr/lib
</span></span><span class="line"><span class="cl">/usr/local
</span></span><span class="line"><span class="cl">/usr/local/bin
</span></span><span class="line"><span class="cl">/usr/local/include
</span></span><span class="line"><span class="cl">/usr/local/sbin
</span></span><span class="line"><span class="cl">/usr/local/share
</span></span><span class="line"><span class="cl">/usr/sbin
</span></span><span class="line"><span class="cl">/usr/share
</span></span><span class="line"><span class="cl">/usr/src
</span></span><span class="line"><span class="cl">/var
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># 配置文件用换行方便编辑，传给 trash-cli 的时候用逗号拼接</span>
</span></span><span class="line"><span class="cl">$ cat ~/.protected_paths <span class="p">|</span> awk <span class="s1">&#39;BEGIN { ORS=&#34;,&#34; } { print $0 }&#39;</span> <span class="p">|</span> sed <span class="s1">&#39;s/,$//&#39;</span>
</span></span><span class="line"><span class="cl">/,/bin,/boot,/dev,/etc,/home,/initrd,/lib,/lib32,/lib64,/proc,/root,/sbin,/sys,/usr,/usr/bin,/usr/include,/usr/lib,/usr/local,/usr/local/bin,/usr/local/include,/usr/local/sbin,/usr/local/share,/usr/sbin,/usr/share,/usr/src,/var
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># 可以把这行加到 ~/.zshrc 里，或者 ~/.bashrc 或者其他...</span>
</span></span><span class="line"><span class="cl">$ <span class="nb">alias</span> <span class="nv">rm</span><span class="o">=</span><span class="s1">&#39;trash --protect $(cat ~/.protected_paths | awk &#39;</span><span class="se">\&#39;</span><span class="s1">&#39;BEGIN { ORS=&#34;,&#34; } { print $0 }&#39;</span><span class="se">\&#39;</span><span class="s1">&#39; | sed &#39;</span><span class="se">\&#39;</span><span class="s1">&#39;s/,$//&#39;</span><span class="se">\&#39;</span><span class="s1">&#39;) &#39;</span></span></span></code></pre></div>
<p>最终效果就是 回收站 + deny list 的<strong>双重保护</strong>，再误删就。。。有点过分了</p>






<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ rm -r /usr/local/bin
</span></span><span class="line"><span class="cl">trash: skipping <span class="s2">&#34;/usr/local/bin&#34;</span></span></span></code></pre></div>
]]></content:encoded></item></channel></rss>